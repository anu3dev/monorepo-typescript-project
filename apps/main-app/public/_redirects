# Netlify _redirects file for single-page application (SPA)
# This file tells Netlify to serve the index.html file for all routes
# so that React Router can handle client-side routing

# Handle all routes by serving index.html (SPA fallback)
/*    /index.html   200

# Optional: Handle specific API routes if you have any
# /api/*  https://your-api-domain.com/api/:splat  200

# Optional: Handle specific file extensions that should be served directly
# This ensures assets like images, CSS, JS files are served correctly
/assets/*  /assets/:splat  200
*.js       /:splat         200
*.css      /:splat         200
*.png      /:splat         200
*.jpg      /:splat         200
*.jpeg     /:splat         200
*.gif      /:splat         200
*.svg      /:splat         200
*.ico      /:splat         200
*.woff     /:splat         200
*.woff2    /:splat         200
*.ttf      /:splat         200
*.eot      /:splat         200